services:
  - type: web
    name: wuziqi-backend
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT miniprogram:app
    envVars:
      - key: FLASK_ENV
        value: production
      - key: PORT
        generateValue: true